var React = require('react');

class Timer extends React.Component {
    render () {
        return (
            <div>
                <h2>Hello Timer</h2>
            </div>
        );
    }
}

module.exports = Timer;
